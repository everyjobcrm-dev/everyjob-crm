// Reserved route for the Notifications / Alerts feature (דף התראות).
// Intentionally not linked from PortalNav yet — the UI ships in a future phase.
// Routing is prepared now so deep links and the eventual nav item can point here directly.

export default function NotificationsPage() {
  return null;
}
